**Summary**
- Updated `test/BtB/plot_summary.ipynb` to replace box plots with line-histogram plots for length distributions (with soma vs no soma), using `numpy.histogram` and line plots.

**Rationale**
- Line-histogram plots provide a simple distribution shape view without additional dependencies.

**Bug Reports / Improvement Suggestions**
- None identified during this change.
